type Message = record {
  id: nat64;
  from: principal;
  to: principal;
  text: text;
  timestamp: nat64;
};

service : {
   "post_message": (principal, text) -> (variant { Ok; Err : text });
  get_conversation: (principal) -> (vec Message) query;
};